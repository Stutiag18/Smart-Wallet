{
  "$schema": "https://railway.app/railway.schema.json",
  "version": "v2",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE"
  },
  "services": {
    "smart-wallet-backend": {
      "source": {
        "directory": "backend"
      },
      "environment": {
        "SPRING_DATASOURCE_URL": "jdbc:postgresql://${{ postgres.POSTGRES_HOST }}:${{ postgres.POSTGRES_PORT }}/${{ postgres.POSTGRES_DATABASE }}",
        "POSTGRES_USER": "${{ postgres.POSTGRES_USER }}",
        "POSTGRES_PASSWORD": "${{ postgres.POSTGRES_PASSWORD }}",
        "MONGODB_URI": "${{ mongodb.MONGODB_URL }}",
        "MONGODB_DATABASE": "smart_wallet_onboarding",
        "REDIS_HOST": "${{ redis.REDIS_HOST }}",
        "REDIS_PORT": "${{ redis.REDIS_PORT }}",
        "REDIS_PASSWORD": "${{ redis.REDIS_PASSWORD }}",
        "HIBERNATE_DDL_AUTO": "update",
        "FLYWAY_ENABLED": "false",
        "SERVER_PORT": "8081"
      }
    }
  }
}
